program exam3
declare a,x,y,i,n,s,z,r enddeclare
	if (a+x)>y then
	 	while i <= n 
	 		s := s + i;
	 		i := i + 1;
	 	endwhile
	else if [x > y or x < z] then 
			z:= 3;
		else
			r := x + y;
		endif
	endif

endprogram